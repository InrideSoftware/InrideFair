<Application x:Class="InrideFair.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="UI/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Цвета -->
            <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#1e1e1e"/>
            <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#2a2a2a"/>
            <SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="#252525"/>
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="#ffffff"/>
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="#cccccc"/>
            <SolidColorBrush x:Key="TertiaryTextBrush" Color="#aaaaaa"/>
            <SolidColorBrush x:Key="QuaternaryTextBrush" Color="#888888"/>

            <!-- Цвета статусов -->
            <SolidColorBrush x:Key="SuccessBrush" Color="#00ff00"/>
            <SolidColorBrush x:Key="WarningBrush" Color="#ffa500"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="#ff6600"/>
            <SolidColorBrush x:Key="InfoBrush" Color="#00aaff"/>
            <SolidColorBrush x:Key="DangerBrush" Color="#ff0000"/>

            <!-- Цвета кнопок -->
            <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#0066cc"/>
            <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#0055aa"/>
            <SolidColorBrush x:Key="DisabledButtonBrush" Color="#555555"/>
            <SolidColorBrush x:Key="DisabledButtonTextBrush" Color="#888888"/>

            <!-- Стили кнопок -->
            <Style x:Key="DarkButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="5"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource PrimaryButtonHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{StaticResource DisabledButtonBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource DisabledButtonTextBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="Padding" Value="40,15"/>
            </Style>

            <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="30,10"/>
            </Style>

            <!-- Стили ProgressBar -->
            <Style TargetType="ProgressBar">
                <Setter Property="Height" Value="20"/>
                <Setter Property="Minimum" Value="0"/>
                <Setter Property="Maximum" Value="100"/>
                <Setter Property="Value" Value="0"/>
                <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryButtonBrush}"/>
            </Style>

            <!-- Стили TextBlock -->
            <Style x:Key="TitleTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="28"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="SubtitleTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="{StaticResource QuaternaryTextBrush}"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="InfoTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
            </Style>

            <Style x:Key="StatusTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>

            <Style x:Key="LogTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>

            <!-- Стили TextBox -->
            <Style x:Key="ReadOnlyTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <Style x:Key="DetailsTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource ReadOnlyTextBoxStyle}">
                <Setter Property="Foreground" Value="{StaticResource TertiaryTextBrush}"/>
                <Setter Property="FontSize" Value="11"/>
            </Style>

            <Style x:Key="LogTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource ReadOnlyTextBoxStyle}">
                <Setter Property="Foreground" Value="{StaticResource QuaternaryTextBrush}"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Height" Value="80"/>
            </Style>

            <!-- Стили Border -->
            <Style x:Key="InfoBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Padding" Value="15"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>

            <Style x:Key="DetailsBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
